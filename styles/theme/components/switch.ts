import type { ComponentStyleConfig } from "@chakra-ui/react";

export const Switch: ComponentStyleConfig = {
  baseStyle: {
    track: {
      _checked: {
        bg: "#688bec",
      },
    },
  },
};
